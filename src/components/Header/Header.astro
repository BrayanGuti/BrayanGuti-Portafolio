<header id="main-header">
  <nav id="main-nav">
    <!-- Menú normal para desktop -->
    <div class="desktop-menu">
      <a data-i18n="header_proyectos" href="#Proyectos"></a>
      <a data-i18n="header_skills" href="#Skills"></a>
      <a data-i18n="header_sobre_mi" href="#Sobre-mi"></a>
      <a data-i18n="header_contactame" href="mailto:brag.863@gmail.com"></a>
      <button class="toggle-lang" id="desktop-toggle-lang"></button>
      <button class="theme-toggle" id="theme-toggle" aria-label="Cambiar tema">🌙</button>
    </div>
    
    <!-- Botón hamburguesa para móvil -->
    <button class="hamburger" id="hamburger" aria-label="Menú">
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
      <span class="hamburger-line"></span>
    </button>
  </nav>
  
  <!-- Menú móvil (sidebar) -->
  <div class="mobile-menu" id="mobile-menu">
    <div class="mobile-menu-content">
      <div class="mobile-menu-header">
        <button class="toggle-lang" id="mobile-toggle-lang"></button>
        <button class="theme-toggle mobile-theme" id="mobile-theme-toggle" aria-label="Cambiar tema">🌙</button>
      </div>
      <a data-i18n="header_proyectos" href="#Proyectos"></a>
      <a data-i18n="header_skills" href="#Skills"></a>
      <a data-i18n="header_sobre_mi" href="#Sobre-mi"></a>
      <a data-i18n="header_contactame" href="mailto:brag.863@gmail.com"></a>
    </div>
  </div>
  <script src="/src/scripts/switch-language.js"></script>
  <script src="/src/scripts/theme.js"></script>
  <script src="/src/scripts/header-fixed.js"></script>
  <script src="/src/scripts/mobile-lateral-nav.js"></script>  
</header>
<span class="space"></span>
<style>
html {
  scroll-behavior: smooth;
}
.space {
  display: block;
  height: 5vh; /* Altura del header */
}

.mobile-menu-header{
  display: flex;
  gap: 10px;
  align-items: center;
  padding: 0 1rem;
  margin-bottom: 20px;
}


header {
  position: fixed;
  width: 100%;
  height: 10vh;
  padding: 0 1rem;
  background-color: var(--background-color);
  display: flex;
  justify-content: center;
  align-items: center;
  z-index: 1000;
}

nav {
  display: flex;
  width: 100%;
  max-width: 1200px;
  justify-content: center;
  align-items: center;
  padding: 0.5rem 1rem;
}

.toggle-lang img{
  width: 24px;
  height: 15px;
  vertical-align: middle;
  margin: 0;
  margin-right: 0;
  padding: 0;
}

.desktop-menu {
  display: flex;
  gap: 0.7rem;
  padding: 0.5rem 1rem;
  border-radius: 9999px;
  background-color: transparent;
  backdrop-filter: blur(8px);
  transition: background-color 0.3s ease;
  align-items: center;
  justify-content: center;
}

.theme-toggle {
  [data-theme="dark"] & {
    padding-bottom: 5px;
  }
}



nav.nav-scrolled .desktop-menu {
  background-color: rgb(255, 255, 255);
  box-shadow: 0 4px 6px rgba(0, 0, 0, 0.4);
  [data-theme="dark"] & {
    background-color: rgb(10, 27, 48);
  }
}

nav a {
  position: relative;
  text-decoration: none;
  color: var(--text-color);
  font-weight: 500;
  transition: color 0.3s ease;
}

nav a::after {
  content: "";
  position: absolute;
  left: 0;
  bottom: -4px;
  width: 100%;
  height: 2px;
  background-color: var(--text-color);
  transform: scaleX(0);
  transform-origin: left;
  transition: transform 0.3s ease;
}

nav a:hover::after {
  transform: scaleX(1);
}

nav a:hover {
  color: var(--verde-auxiliar);
}

button {
  display: flex;
  justify-content: center;
  align-items: center;
  cursor: pointer;
  background-color: transparent;
  font-size: 1.2rem;
  border: none;
}

/* Estilos para el menú hamburguesa */
.hamburger {
  display: none;
  flex-direction: column;
  justify-content: space-between;
  width: 30px;
  height: 21px;
  background: transparent;
  border: none;
  cursor: pointer;
  padding: 0;
  margin-right: 2.5vw;
  z-index: 1100;
}

.hamburger-line {
  width: 100%;
  height: 3px;
  background-color: var(--text-color);
  transition: all 0.3s ease;
}

/* Menú móvil (sidebar) */
.mobile-menu {
  position: fixed;
  top: 0;
  right: -100%;
  width: 70%;
  max-width: 300px;
  height: 100vh;
  background-color: #c6c8ca;
  border-left: 1px solid #011b36;
  box-shadow: -5px 0 15px rgba(0, 0, 0, 0.1);
  transition: right 0.3s ease;
  z-index: 1050;
  padding-top: 80px;
  [data-theme="dark"] & {
    background-color: #03080f;
    border-left: 1px solid #011b36;
  }
}


.mobile-menu-content {
  display: flex;
  flex-direction: column;
  padding: 20px;
  gap: 25px;
}

.mobile-menu a {
  color: var(--text-color);
  text-decoration: none;
  font-size: 1.2rem;
  padding: 10px 0;
  border-bottom: 1px solid rgba(0, 0, 0, 0.1);
  transition: color 0.3s ease;
  [data-theme="dark"] & {
    border-bottom: 1px solid rgb(182, 178, 178);
  }
}

.mobile-menu a:hover {
  color: var(--verde-auxiliar);
}

.mobile-theme {
  width: min-content;
  display: flex;
  justify-content: center;
  align-items: center;
  gap: 5px;
  color: var(--text-color);
  font-size: 1.2rem;
}

.mobile-theme span {
  font-size: 1.5rem;
  margin-bottom: 6px;
}

/* Estilos cuando el menú está abierto */
.mobile-menu.open {
  right: 0;
}

.hamburger.open .hamburger-line:nth-child(1) {
  transform: translateY(9px) rotate(45deg);
}

.hamburger.open .hamburger-line:nth-child(2) {
  opacity: 0;
}

.hamburger.open .hamburger-line:nth-child(3) {
  transform: translateY(-9px) rotate(-45deg);
}

/* Media queries para responsividad */
@media (max-width: 768px) {
  header {
    height: 8vh;
    padding: 0 0.5rem;
    justify-content: flex-end;
  }
  
  .desktop-menu {
    display: none;
  }
  
  .hamburger {
    display: flex;
  }
  
  nav {
    padding: 0.5rem;
    justify-content: flex-end;
  }
  

  
  .mobile-menu.nav-scrolled {
    background-color: rgb(255, 255, 255);
    [data-theme="dark"] & {
      background-color: rgb(10, 27, 48);
    }
  }
}
</style>