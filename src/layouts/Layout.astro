<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=IBM+Plex+Sans:ital,wght@0,100..700;1,100..700&display=swap"
      rel="stylesheet"
    />
    <meta
      name="description"
      content="Portafolio de Brayan Guti, ingeniero de software especializado en desarrollo frontend con conocimientos en backend y bases de datos."
    />
    <meta
      name="keywords"
      content="Brayan Guti, Desarrollador Frontend, Ingeniero de Software, Desarrollo Web, Portafolio"
    />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="generator" content={Astro.generator} />
    <title>{Astro.props.pageTitle}</title>
  </head>

  <body>
    <div class="cursor-circle" id="cursorCircle"></div>
    <slot />
    <script src="/src/scripts/cursor-gradient.js"></script>
  </body>
</html>

<style>
  html {
    box-sizing: border-box;
  }
  *,
  *:before,
  *:after {
    box-sizing: inherit;
  }

  html,
  body {
    font-family: "IBM Plex Sans", serif;
    margin: 0;
    width: 100%;
    height: 100%;
  }

  :root {
    --bg-color: rgb(212, 201, 201);
    --text-color: #0e0d0d;


    --verde-auxiliar: #1E4FB2;
    --amarillo-auxiliar: #EC4E20;
    --buttons-color: rgba(255, 255, 255, 0.6);


    --circle-color: rgb(255, 255, 255);
    --circle-color2: rgb(255, 255, 255);
    --circle-color3: rgb(255, 255, 255);
  }

  [data-theme="light"] body {
    background-color: var(--bg-color);
    color: var(--text-color);
    
    background-image: 
      linear-gradient(120deg, rgba(255,255,255,0.6) 0%, rgba(240,240,240,0.9) 100%),
      radial-gradient(circle at 20% 30%, rgba(255, 255, 255, 0.2) 0%, transparent 80%),
      radial-gradient(circle at 80% 70%, rgba(230, 230, 230, 0.2) 0%, transparent 80%);
    
    background-repeat: no-repeat;
    background-attachment: fixed;
    background-size: cover;
    background-blend-mode: lighten;
    position: relative;
    z-index: 0;
  }
  
  [data-theme="light"] body::before {
    content: "";
    position: fixed;
    top: -10%;
    left: -10%;
    width: 120vw;
    height: 120vh;
    background-image: repeating-linear-gradient(
      45deg,
      rgba(220, 220, 220, 0.103),
      rgba(220, 220, 220, 0.534) 1px,
      transparent 2px,
      transparent 20px
    );
    z-index: -2;
    pointer-events: none;
    animation: gentleDrift 1s ease-in-out infinite alternate;
    opacity: 0.4;
  }
  
  @keyframes gentleDrift {
    0% {
      transform: translate(0, 0) rotate(0deg);
    }
    100% {
      transform: translate(5px, 10px) rotate(0.5deg);
    }
  }

  [data-theme="dark"] {
		--bg-color: #020a19; 
    --text-color: #EAE8FF;

    --verde-auxiliar: #1DD3B0;
    --amarillo-auxiliar: #FFF07C;
    --azul-claro: #2196F3 ;
    --buttons-color: rgba(43, 48, 54, 0.5);

    --circle-color: rgba(45, 57, 216, 0.719);
    --circle-color2: rgba(10, 35, 67, 0.6);
    --circle-color3: rgba(0, 0, 0, 0.9);

	}


  .cursor-circle {
	  position: fixed;
	  width: 170px;
	  height: 170px;
	  border-radius: 50%;
	  pointer-events: none;
	  transform: translate(-50%, -50%);
	  z-index: -1;
	  mix-blend-mode: screen;
	  transition: transform 0.05s ease;
	  opacity: 0.7;
	  filter: blur(12px);
	  background: radial-gradient(
	    circle,
	    var(--circle-color) 0%,
	    var(--circle-color2) 25%,
	    var(--circle-color3) 35%,
	    var(--circle-color2) 50%,
	    var(--circle-color3) 60%,
	    var(--circle-color2) 75%,
	    var(--circle-color3) 85%,
	    transparent 100%
	  );
	}

	[data-theme="light"] .cursor-circle {
	  display: none;
}


[data-theme="dark"] body {
  background-color: var(--bg-color);
  color: var(--text-color);

  background-image:
  radial-gradient(
    ellipse at center,
    rgba(120, 200, 255, 0.7) 0%,        /* 💡 Azul muy claro, casi luz */
    rgba(60, 130, 200, 0.85) 15%,       /* Azul cielo intermedio */
    rgba(20, 60, 110, 0.95) 35%,        /* Empieza la profundidad */
    rgba(10, 30, 60, 0.98) 60%,         /* Oscuro y frío */
    rgba(2, 15, 30, 1) 80%,             /* Azul casi negro */
    #000 100%                           /* Negro absoluto en el borde */
  ),
  radial-gradient(
    circle at 70% 80%,
    rgba(0, 200, 255, 0.1),
    transparent 60%
  );

  background-repeat: no-repeat;
  background-attachment: fixed;
  background-size: 100% 100%;
  background-blend-mode: soft-light, overlay;
  position: relative;
  z-index: 0;
}


[data-theme="dark"] body {
  background-color: var(--bg-color);
  color: var(--text-color);

  background-image:
    radial-gradient(
      ellipse at center,
      rgba(10, 60, 90, 0.5) 0%,
      rgba(5, 30, 60, 0.8) 30%,
      rgba(2, 15, 30, 0.95) 60%,
      rgba(0, 5, 15, 1) 100%
    ),
    radial-gradient(
      circle at 70% 80%,
      rgba(0, 180, 255, 0.06),
      transparent 60%
    );

  background-repeat: no-repeat;
  background-attachment: fixed;
  background-size: 100% 100%;
  background-blend-mode: soft-light, overlay;
  position: relative;
  z-index: 0;
}

[data-theme="dark"] body::before {
  content: "";
  position: fixed;
  top: 0;
  left: 0;
  width: 100vw;
  height: 100vh;
  z-index: -2;
  pointer-events: none;

  background-image: repeating-radial-gradient(
    circle at 40% 60%,
    rgba(0, 180, 255, 0.04) 0px,
    rgba(0, 180, 255, 0.06) 4px,
    transparent 8px,
    transparent 14px
  );

  background-repeat: repeat;
  background-size: 100% auto; /* importante para evitar "bloques" */
  filter: blur(5.5px);
  opacity: 0.6;
  animation: spiralDrift 2s ease-in-out infinite alternate;
}

@keyframes underwaterMotion {
  0% {
    background-position: 0 0;
  }
  100% {
    background-position: 20px 50px;
  }
}

@keyframes spiralDrift {
  0% {
    background-position: 0 0;
  }
  100% {
    background-position: 40px 60px;
  }
}

</style>
